---
import Base from '../../layouts/Base.astro';
import App from '../../admin/App';

export function getStaticPaths() {
  const rutas = [
    undefined, // /admin
    'calculadora',
    'solicitudes',
    'viajes',
    'viaje',
    'vehiculos',
    'conductores',
    'peajes',
    'configuracion',
  ];
  return rutas.map((app) => ({ params: { app } }));
}
---
<Base title="Panel de administración">
  <App client:only="react" />
</Base>